export interface IProduct {
  _id: string;
  title: string;
  author: string;
  price: number;
  category: string;
  quantity: number;
  inStock: boolean;
  description: string;
  image: string;
  // inStock:boolean
}
